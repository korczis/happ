module External.AccelerateTests (
    tests
) where

import Test.Tasty (testGroup)
import Test.Tasty.HUnit(assertEqual, testCase)

tests = testGroup
    "Accelerate Tests"
    [
    ]

--isAllDigitsTrueTest = testCase "Accelerate" $ do
--    assertEqual "" True (1 + 2 == 3)